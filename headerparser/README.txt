The purpose of this project is to put everything in the right place, so to allow 
easy access to the newest build from within other software

* Dll files are copied to build
* hpp files are copied to include
* lib files are copied to lib

The binary is executed as an extra build step when compiling YSE