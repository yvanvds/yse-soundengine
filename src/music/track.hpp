#pragma once
#include "instruments/instrument.hpp"
/*
namespace YSE {
  namespace MUSIC {

    class trackImpl;
    class API track {
    public:
      track(Int interval);
     ~track();
      track& start ();
      track& stop  ();
      Bool   status();
      track& addInstrument(YSE::INSTRUMENTS::instrument & instrument);
      track& remInstrument(YSE::INSTRUMENTS::instrument & instrument);
      virtual void update() = 0;
    private:
      trackImpl * pimpl;
    };

  } // end MUSIC
}   // end YSE
*/