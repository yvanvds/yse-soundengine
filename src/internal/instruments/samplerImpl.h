#pragma once
#include "baseInstrumentImpl.h"
/*

namespace YSE {
  namespace INSTRUMENTS {
    
    class samplerImpl : public baseInstrumentImpl {
    public:

      void create(const char * fileName, Int voices, Int pitch, channel& parent); // filename of your sample, max nr of simultanious voices, normal pitch of your sample (sample will sound at normal speed if this note is played), channel is Global channel if NULL

      Int pitch; // base pitch of the sample
      Bool loop;
      
      Flt getSpeed(Flt pitch); // convert pitch to sample speed
     
      samplerImpl() : loop(false) {}

      void updateVoices();
    };


  }
}
      
      */