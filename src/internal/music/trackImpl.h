#pragma once
#include <vector>
#include "music/track.hpp"
#include "internal/instruments/baseInstrumentImpl.h"

/*
namespace YSE {
  namespace MUSIC {

    class trackImpl {
    public:
      aBool status; // 0 = stopped, 1 = running
      Int interval;
      Int step;
      void update();
      void allNotesOff();

      track * link;


      trackImpl(track * value, Int interval);
     ~trackImpl();
     std::vector<YSE::INSTRUMENTS::baseInstrumentImpl *> instruments;
    };

    
  } // end MUSIC
} // end YSE

*/